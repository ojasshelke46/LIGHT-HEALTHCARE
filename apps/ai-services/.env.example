# OpenAI API key — not required yet: all endpoints below are deterministic
# mocks (no real model calls). Populate this when V2-04 (real model calls)
# lands.
OPENAI_API_KEY=

# Comma-separated list of allowed CORS origins. No wildcard in code (T-05-03).
CORS_ORIGINS=http://localhost:3000
