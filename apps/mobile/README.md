# apps/mobile — Patient app

Expo / React Native, PWA-first. Scaffolded in **Phase 3** (booking + check-in).

Not initialized yet. When starting:

```bash
cd apps/mobile
pnpm create expo-app@latest . --template
```

Consumes `@light/shared-types` for DB types.
