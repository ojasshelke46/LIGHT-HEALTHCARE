---
phase: 03-diagnostics-pharmacy-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/theme.ts
  - apps/web/src/lib/status.ts
  - apps/web/src/lib/results.ts
  - apps/web/src/lib/results.test.ts
  - apps/web/src/lib/time.ts
  - apps/web/src/components/result-link.tsx
  - apps/web/src/app/doctor/consult/[appointmentId]/consult-client.tsx
  - apps/web/src/app/doctor/patients/[id]/page.tsx
autonomous: true
requirements: [DIAG-02, DIAG-03]

must_haves:
  truths:
    - "Lab-tech portal nav shows Pending / In Progress / Completed; pharmacist portal nav shows Pending / Inventory / Dispensed"
    - "A View-result control resolves a stored Storage PATH into a short-lived signed URL on click (private bucket, never precomputed)"
    - "A legacy http(s) result_url still opens directly (backward compatible with seeded order f0..01)"
    - "Doctor history tabs open results through the same signed-URL helper, not a raw href to a private path"
    - "A shared helper converts from/to date-input values into IST day-bound ISO instants for completed/dispensed filters"
  artifacts:
    - path: "apps/web/src/lib/results.ts"
      provides: "getResultUrl(pathOrUrl) — http passthrough vs createSignedUrl(scan-results, 3600)"
      exports: ["getResultUrl"]
    - path: "apps/web/src/components/result-link.tsx"
      provides: "ResultLink client component: View-result button that resolves + opens a signed URL"
      exports: ["ResultLink"]
    - path: "apps/web/src/lib/status.ts"
      provides: "ORDER_STATUS_BADGE, PRESCRIPTION_STATUS_BADGE, ORDER_TYPE_ICON, stockLevel"
      contains: "ORDER_STATUS_BADGE"
    - path: "apps/web/src/lib/time.ts"
      provides: "istRangeFromDates(from,to) IST day bounds for date-range filters"
      contains: "istRangeFromDates"
    - path: "apps/web/src/lib/theme.ts"
      provides: "lab_tech + pharmacist nav arrays (D-32/D-37)"
      contains: "in-progress"
  key_links:
    - from: "apps/web/src/components/result-link.tsx"
      to: "apps/web/src/lib/results.ts"
      via: "getResultUrl(pathOrUrl) on click"
      pattern: "getResultUrl"
    - from: "apps/web/src/app/doctor/patients/[id]/page.tsx"
      to: "apps/web/src/components/result-link.tsx"
      via: "<ResultLink pathOrUrl={order.result_url} />"
      pattern: "ResultLink"
    - from: "apps/web/src/app/doctor/consult/[appointmentId]/consult-client.tsx"
      to: "apps/web/src/components/result-link.tsx"
      via: "<ResultLink pathOrUrl={o.result_url} />"
      pattern: "ResultLink"
---

<objective>
Shared foundation for both Phase-3 portals: role nav (D-32 lab_tech, D-37 pharmacist), the signed-URL result helper (D-35) plus a reusable ResultLink button, the status/type-icon/stock display helpers, and a shared IST date-range helper every diagnostics & pharmacy list consumes. Also retrofits the two doctor history render sites to open results through the signed-URL helper (quality-gate item: D-35 must be adopted in doctor tabs too).

Purpose: Every downstream Wave-2/3 plan (03-02..03-05) builds against these contracts instead of re-deriving nav entries, badge maps, icon maps, stock logic, date-range bounds, and result-URL handling. Interface-first so executors receive the contracts, not a scavenger hunt.
Output: theme.ts nav, status.ts helpers, time.ts date-range helper, results.ts + results.test.ts, result-link.tsx, and the D-35 doctor refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-diagnostics-pharmacy-flow/03-CONTEXT.md

<interfaces>
<!-- Executor: use these directly, no codebase exploration needed. -->

Existing status.ts pattern (LITERAL Tailwind class strings — never interpolated):
```typescript
export const APPOINTMENT_STATUS_BADGE: Record<AppointmentStatus, { label: string; className: string }> = { ... };
```

Existing time.ts (extend, keep IST_OFFSET_MS convention):
```typescript
const IST_OFFSET_MS = 5.5 * 60 * 60 * 1000;
export function todayISTRange(): { startISO: string; endISO: string };
```

Enums (packages/shared-types/src/database.types.ts):
```typescript
order_status: "ordered" | "in_progress" | "completed"
order_type: "lab" | "ct" | "mri" | "xray"
prescription_status: "pending" | "dispensed"
```

Browser Supabase client (apps/web/src/lib/supabase/client.ts):
```typescript
export function createClient(): SupabaseClient<Database>; // createBrowserClient
// storage: createClient().storage.from("scan-results").createSignedUrl(path, 3600) -> { data: { signedUrl } | null, error }
```

D-35 refactor targets (raw <a href> to be replaced):
- apps/web/src/app/doctor/patients/[id]/page.tsx line ~241: `{order.result_url ? (<a href={order.result_url} ...>View result</a>) : null}`
- apps/web/src/app/doctor/consult/[appointmentId]/consult-client.tsx line ~323: `{o.result_url ? (<a href={o.result_url} ...>View result</a>) : null}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getResultUrl helper (D-35) + results.test.ts</name>
  <files>apps/web/src/lib/results.ts, apps/web/src/lib/results.test.ts</files>
  <read_first>
    - apps/web/src/lib/supabase/client.ts (createClient signature + storage access)
    - apps/web/src/lib/hooks/use-realtime.test.ts (vitest style already in repo)
    - .planning/phases/03-diagnostics-pharmacy-flow/03-CONTEXT.md (D-35 exact spec)
  </read_first>
  <behavior>
    - getResultUrl("https://example.com/r.pdf") resolves to "https://example.com/r.pdf" (legacy http passthrough — no Storage call). Matches seeded order f0..01.
    - getResultUrl("http://x/y") resolves to the same http string unchanged.
    - getResultUrl("orders/abc/123-scan.png") calls storage.from("scan-results").createSignedUrl(path, 3600) and resolves to its signedUrl.
    - getResultUrl(null | "") resolves to null.
    - On createSignedUrl error, resolves to null (caller shows a toast).
  </behavior>
  <action>
    Create `getResultUrl(pathOrUrl: string | null): Promise<string | null>` in apps/web/src/lib/results.ts (D-35). Logic: if `!pathOrUrl` return null; if `/^https?:\/\//i.test(pathOrUrl)` return it as-is (legacy seed rows); otherwise `const { data, error } = await createClient().storage.from("scan-results").createSignedUrl(pathOrUrl, 3600); return error ? null : (data?.signedUrl ?? null);`. Client-side on demand only — do NOT precompute or cache. Signed URL TTL is exactly 3600s (D-35).
    Write results.test.ts (vitest): assert the http-passthrough and null/empty branches with a mocked createClient (vi.mock("@/lib/supabase/client")) whose storage.from().createSignedUrl returns a stub; assert the path branch calls createSignedUrl with (path, 3600) and returns the stub signedUrl, and returns null when the stub returns an error.
  </action>
  <verify>
    <automated>pnpm --filter @light/web test -- results.test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "createSignedUrl(pathOrUrl, 3600)" apps/web/src/lib/results.ts` (3600s TTL present)
    - `grep -Eq "https\?:" apps/web/src/lib/results.ts` (http-passthrough branch exists)
    - `grep -q "scan-results" apps/web/src/lib/results.ts`
    - `pnpm --filter @light/web test -- results.test` exits 0
  </acceptance_criteria>
  <done>Unit tests pass; getResultUrl returns http URLs verbatim and signs Storage paths with a 3600s TTL.</done>
</task>

<task type="auto">
  <name>Task 2: ResultLink component + status/icon/stock + date-range helpers</name>
  <files>apps/web/src/components/result-link.tsx, apps/web/src/lib/status.ts, apps/web/src/lib/time.ts</files>
  <read_first>
    - apps/web/src/lib/status.ts (extend, keep LITERAL Tailwind convention + existing exports)
    - apps/web/src/lib/time.ts (extend, reuse IST_OFFSET_MS + todayISTRange shape)
    - apps/web/src/lib/theme.ts (lucide import style for ORDER_TYPE_ICON)
    - apps/web/src/components/empty-state.tsx (client-component + toast style)
  </read_first>
  <action>
    (a) result-link.tsx — "use client" component `ResultLink({ pathOrUrl }: { pathOrUrl: string | null })`. If `!pathOrUrl` return null. Local `loading` state; on click: setLoading(true), `const url = await getResultUrl(pathOrUrl)` (import from "@/lib/results"), setLoading(false); if `!url` `toast.error("Could not open result")` (sonner) and return; else `window.open(url, "_blank", "noopener,noreferrer")`. Render a `<button type="button" disabled={loading} className="text-blue-600 hover:underline">` showing "Opening…" while loading else "View result". Add `data-testid="view-result"`.
    (b) status.ts — append (LITERAL class strings, same rule as APPOINTMENT_STATUS_BADGE):
      - `ORDER_STATUS_BADGE: Record<OrderStatus, { label; className }>` — ordered → {"Ordered","bg-slate-100 text-slate-700"}, in_progress → {"In Progress","bg-amber-100 text-amber-800"}, completed → {"Completed","bg-green-100 text-green-800"}. Type `OrderStatus = Database["public"]["Enums"]["order_status"]`.
      - `PRESCRIPTION_STATUS_BADGE: Record<PrescriptionStatus, ...>` — pending → {"Pending","bg-yellow-100 text-yellow-800"}, dispensed → {"Dispensed","bg-green-100 text-green-800"}.
      - `ORDER_TYPE_ICON: Record<OrderType, LucideIcon>` — lab: FlaskConical, ct: ScanLine, mri: Magnet, xray: ImageIcon (import `{ FlaskConical, ScanLine, Magnet, Image as ImageIcon }` and `type { LucideIcon }` from "lucide-react") (D-33).
      - `stockLevel(stock: number, quantity: number, threshold: number): { level: "insufficient" | "low" | "ok"; label: string; className: string }` (D-38) — stock < quantity → {"insufficient", `Insufficient stock (have ${stock})`, "bg-red-100 text-red-800"}; else stock <= threshold → {"low","Low stock","bg-amber-100 text-amber-800"}; else {"ok", `In stock (${stock})`, "bg-green-100 text-green-800"}.
    (c) time.ts — add `istRangeFromDates(from?: string | null, to?: string | null): { startISO: string | null; endISO: string | null }` (D-36/D-41). `from`/`to` are YYYY-MM-DD from date inputs. startISO = start of the `from` IST day (inclusive) or null; endISO = start of the IST day AFTER `to` (exclusive) or null. Reuse the IST_OFFSET_MS math from todayISTRange (Date.UTC of the parsed y/m/d minus IST_OFFSET_MS; +24h for the day-after). Return nulls for absent inputs so callers apply only the bounds provided.
  </action>
  <verify>
    <automated>pnpm --filter @light/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "getResultUrl" apps/web/src/components/result-link.tsx` and `grep -q "noopener,noreferrer" apps/web/src/components/result-link.tsx`
    - `grep -q "data-testid=\"view-result\"" apps/web/src/components/result-link.tsx`
    - `grep -q "ORDER_STATUS_BADGE" apps/web/src/lib/status.ts` and `grep -q "PRESCRIPTION_STATUS_BADGE" apps/web/src/lib/status.ts`
    - `grep -q "ORDER_TYPE_ICON" apps/web/src/lib/status.ts` and `grep -q "stockLevel" apps/web/src/lib/status.ts`
    - `grep -q "Insufficient stock (have" apps/web/src/lib/status.ts`
    - `grep -q "istRangeFromDates" apps/web/src/lib/time.ts`
    - `pnpm --filter @light/web typecheck` exits 0
  </acceptance_criteria>
  <done>ResultLink resolves + opens signed URLs via getResultUrl; status.ts exports the four new helpers with literal Tailwind classes; time.ts exports istRangeFromDates.</done>
</task>

<task type="auto">
  <name>Task 3: Portal nav (D-32/D-37) + adopt ResultLink in doctor tabs (D-35)</name>
  <files>apps/web/src/lib/theme.ts, apps/web/src/app/doctor/patients/[id]/page.tsx, apps/web/src/app/doctor/consult/[appointmentId]/consult-client.tsx</files>
  <read_first>
    - apps/web/src/lib/theme.ts (current lab_tech + pharmacist nav are single-entry placeholders)
    - apps/web/src/app/doctor/patients/[id]/page.tsx lines 236-252 (result_url render site)
    - apps/web/src/app/doctor/consult/[appointmentId]/consult-client.tsx lines 316-336 (result_url render site)
  </read_first>
  <action>
    (a) theme.ts — replace the placeholder lab_tech.nav with three entries (D-32): `{ href: "/diagnostics", label: "Pending", icon: FlaskConical }`, `{ href: "/diagnostics/in-progress", label: "In Progress", icon: Loader }`, `{ href: "/diagnostics/completed", label: "Completed", icon: CheckCircle2 }`. Replace pharmacist.nav with three entries (D-37): `{ href: "/pharmacy", label: "Pending", icon: Pill }`, `{ href: "/pharmacy/inventory", label: "Inventory", icon: Package }`, `{ href: "/pharmacy/dispensed", label: "Dispensed", icon: ClipboardList }`. Add `Loader, CheckCircle2, Package, ClipboardList` to the lucide import.
    (b) Replace the raw `<a href={order.result_url} ...>View result</a>` in doctor/patients/[id]/page.tsx with `<ResultLink pathOrUrl={order.result_url} />` (import ResultLink from "@/components/result-link"). This is a server component rendering a client child — allowed.
    (c) Replace the raw `<a href={o.result_url} ...>View result</a>` in consult-client.tsx with `<ResultLink pathOrUrl={o.result_url} />` (import ResultLink). Remove now-unused `target`/`rel` anchor code at both sites. Do NOT change the surrounding conditional (still guarded by result_url presence inside ResultLink).
  </action>
  <verify>
    <automated>pnpm --filter @light/web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "/diagnostics/in-progress" apps/web/src/lib/theme.ts` and `grep -q "/diagnostics/completed" apps/web/src/lib/theme.ts`
    - `grep -q "/pharmacy/inventory" apps/web/src/lib/theme.ts` and `grep -q "/pharmacy/dispensed" apps/web/src/lib/theme.ts`
    - `grep -q "ResultLink" apps/web/src/app/doctor/patients/[id]/page.tsx` and `grep -q "ResultLink" apps/web/src/app/doctor/consult/[appointmentId]/consult-client.tsx`
    - `! grep -q "href={order.result_url}" apps/web/src/app/doctor/patients/[id]/page.tsx` (raw anchor removed)
    - `! grep -q "href={o.result_url}" apps/web/src/app/doctor/consult/[appointmentId]/consult-client.tsx`
    - `pnpm --filter @light/web build` exits 0
  </acceptance_criteria>
  <done>Both portal navs match D-32/D-37; doctor history + consult result links open via ResultLink (signed URLs), no raw private-path anchors remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase Storage | Result files live in the private `scan-results` bucket; access is via time-limited signed URLs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | getResultUrl signed URLs | mitigate | TTL fixed at 3600s; resolved on demand per click, never precomputed into lists or logged (D-35) |
| T-03-02 | Information Disclosure | ResultLink window.open | accept | Opens with rel `noopener,noreferrer`; URL already scoped to the authenticated caller's session grant, short-lived |
| T-03-03 | Tampering | getResultUrl input | accept | pathOrUrl originates only from the trusted orders.result_url DB column (RLS-scoped read), never user free-text |
</threat_model>

<verification>
- `pnpm --filter @light/web typecheck` and `pnpm --filter @light/web build` both pass.
- `pnpm --filter @light/web test -- results.test` passes (getResultUrl branches).
- Manual grep: no raw `<a href={...result_url}>` remains in doctor pages.
</verification>

<success_criteria>
- Lab-tech and pharmacist navs render the three D-32 / D-37 entries.
- getResultUrl passes http(s) through and signs Storage paths (3600s); ResultLink opens the result in a new tab or toasts on failure.
- istRangeFromDates returns IST day bounds for from/to date filters.
- Doctor history/consult tabs open results through ResultLink; typecheck + build green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-diagnostics-pharmacy-flow/03-01-SUMMARY.md`
</output>
</content>
