---
phase: 03-diagnostics-pharmacy-flow
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/web/src/app/diagnostics/completed/page.tsx
  - apps/web/src/app/diagnostics/completed/completed-filters.tsx
  - apps/web/e2e/diagnostics-flow.spec.ts
autonomous: true
requirements: [DIAG-03]

must_haves:
  truths:
    - "Lab tech browses completed orders (status=completed), newest first, with patient, type, doctor, completed time"
    - "Date-range (from/to) and order-type filters narrow the list, applied server-side"
    - "Each row opens its result file via the signed-URL ResultLink (handles both Storage paths and legacy http URLs)"
  artifacts:
    - path: "apps/web/src/app/diagnostics/completed/page.tsx"
      provides: "Server component: filtered completed-orders query via searchParams"
      contains: "status"
    - path: "apps/web/src/app/diagnostics/completed/completed-filters.tsx"
      provides: "Client from/to date + type filter controls that push query params"
      contains: "useRouter"
  key_links:
    - from: "apps/web/src/app/diagnostics/completed/page.tsx"
      to: "orders (status=completed) filtered by istRangeFromDates + type"
      via: "server query with searchParams"
      pattern: "istRangeFromDates"
    - from: "apps/web/src/app/diagnostics/completed/page.tsx"
      to: "apps/web/src/components/result-link.tsx"
      via: "<ResultLink pathOrUrl={row.result_url} />"
      pattern: "ResultLink"
---

<objective>
The diagnostics browse slice (DIAG-03): a server-rendered Completed page listing finished orders newest-first, with server-side date-range and order-type filters, each row opening its result through the signed-URL ResultLink. Completes the diagnostics portal and extends the diagnostics e2e to prove the signed-URL View path end-to-end.

Purpose: Lab techs (and anyone auditing) can find a past result by date/type and open it safely from the private bucket.
Output: /diagnostics/completed page + filter controls; the diagnostics-flow e2e extended through the completed page + View.
</objective>

## Phase Goal

**As a** lab tech, **I want to** filter completed orders by date and type and open their result files, **so that** I can retrieve any past test result on demand.

<sub>Derived from ROADMAP Phase-3 Success Criteria 2 (ROADMAP `Goal:` line is prose; this slice scopes lab-tech browse).</sub>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-diagnostics-pharmacy-flow/03-CONTEXT.md
@apps/web/src/app/reception/billing/billing-client.tsx

<interfaces>
<!-- From 03-01 + 03-02. Use directly. -->
Server Supabase client (apps/web/src/lib/supabase/server.ts):
```typescript
export async function createClient(): Promise<SupabaseClient<Database>>; // reads auth cookie
```
apps/web/src/lib/time.ts (03-01):
```typescript
export function istRangeFromDates(from?: string|null, to?: string|null): { startISO: string|null; endISO: string|null };
```
apps/web/src/lib/status.ts (03-01): ORDER_TYPE_ICON (lab/ct/mri/xray).
apps/web/src/components/result-link.tsx (03-01): `<ResultLink pathOrUrl={string|null} />` (testid view-result).
Seed: completed order f0..01 has a legacy http result_url ("https://example.com/r.pdf"); 03-02's e2e creates + completes order f0..e2 with a Storage-path result_url.
order_type enum: "lab" | "ct" | "mri" | "xray".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Completed page (server, filtered) + filter controls (DIAG-03)</name>
  <files>apps/web/src/app/diagnostics/completed/page.tsx, apps/web/src/app/diagnostics/completed/completed-filters.tsx</files>
  <read_first>
    - apps/web/src/app/doctor/patients/[id]/page.tsx (async server component + createClient() from @/lib/supabase/server + nested-select cast)
    - apps/web/src/lib/time.ts (istRangeFromDates)
    - apps/web/src/app/reception/queue-client.tsx (Input/Select/Table + EmptyState imports)
    - .planning/phases/03-diagnostics-pharmacy-flow/03-CONTEXT.md (D-36 exact spec)
  </read_first>
  <action>
    page.tsx — async server component. Signature `{ searchParams }: { searchParams: Promise<{ from?: string; to?: string; type?: string }> }`; `const { from, to, type } = await searchParams`. `const supabase = await createClient()` (from "@/lib/supabase/server"). Build query: `.from("orders").select("id, type, instructions, result_url, result_notes, completed_at, patients(name), visits(doctors(staff(name)))").eq("status", "completed").order("completed_at", { ascending: false })`. Apply filters:
      - `const VALID_TYPES = ["lab","ct","mri","xray"] as const;` — only `if (type && VALID_TYPES.includes(type)) query = query.eq("type", type)` (whitelist — never pass an unvalidated type through, T-03-08).
      - `const { startISO, endISO } = istRangeFromDates(from, to); if (startISO) query = query.gte("completed_at", startISO); if (endISO) query = query.lt("completed_at", endISO);`
    Cast result rows `as CompletedOrder[] | null` (nested-select quirk). Render `<CompletedFilters />` (pass current from/to/type as defaults) then a Table: type icon (ORDER_TYPE_ICON), patient name, doctor (`row.visits?.doctors?.staff?.name`), `formatIST(row.completed_at, "datetime")`, result_notes, and `<ResultLink pathOrUrl={row.result_url} />`. EmptyState (icon CheckCircle2, "No completed orders") when empty; error branch on query error.
    completed-filters.tsx — "use client". `useRouter` + `useSearchParams`. Two date inputs (data-testid `filter-from`, `filter-to`) and a type `<Select>` (data-testid `filter-type`) with options All/lab/ct/mri/xray. On any change, build a URLSearchParams from the current values (omit empties) and `router.push(\`/diagnostics/completed?${params.toString()}\`)`. Default the inputs from props (the server-parsed values) so the controls stay in sync with the URL.
  </action>
  <verify>
    <automated>pnpm --filter @light/web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'eq("status", "completed")' apps/web/src/app/diagnostics/completed/page.tsx`
    - `grep -q "istRangeFromDates" apps/web/src/app/diagnostics/completed/page.tsx` and `grep -q "VALID_TYPES" apps/web/src/app/diagnostics/completed/page.tsx`
    - `grep -q "ResultLink" apps/web/src/app/diagnostics/completed/page.tsx`
    - `grep -q "useRouter" apps/web/src/app/diagnostics/completed/completed-filters.tsx` and `grep -q "filter-type" apps/web/src/app/diagnostics/completed/completed-filters.tsx`
    - `pnpm --filter @light/web build` exits 0
  </acceptance_criteria>
  <done>/diagnostics/completed lists completed orders newest-first with working server-side date + type filters and per-row ResultLink; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend diagnostics-flow e2e through Completed + signed-URL View</name>
  <files>apps/web/e2e/diagnostics-flow.spec.ts</files>
  <read_first>
    - apps/web/e2e/diagnostics-flow.spec.ts (03-02 — extend the SAME spec; cleanup lives at the end)
    - .planning/phases/03-diagnostics-pharmacy-flow/03-CONTEXT.md (D-43: "completed page shows row + signed-url View works")
  </read_first>
  <action>
    After the Mark-Complete step (order f0..e2 now completed), add steps to the SAME spec:
      - Navigate to /diagnostics/completed; assert a row for the 'CBC E2E' order (f0..e2) is visible.
      - Scope to that row and click its View-result control (`getByTestId("view-result")`). Set up `const popupPromise = page.waitForEvent("popup")` before the click; after, `const popup = await popupPromise; expect(popup.url()).toContain("scan-results")` (a signed URL to the private bucket, proving getResultUrl signed the Storage path). Close the popup.
    Keep the existing self-reset cleanup as the final step (delete order f0..e2 + storage.remove the uploaded path) so repeated runs stay idempotent (D-43). Do not remove the earlier accept/upload/complete assertions.
  </action>
  <verify>
    <automated>pnpm --filter @light/web exec playwright test e2e/diagnostics-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "/diagnostics/completed" apps/web/e2e/diagnostics-flow.spec.ts`
    - `grep -q "waitForEvent(\"popup\")" apps/web/e2e/diagnostics-flow.spec.ts` and `grep -q "view-result" apps/web/e2e/diagnostics-flow.spec.ts`
    - `grep -q "scan-results" apps/web/e2e/diagnostics-flow.spec.ts`
    - `pnpm --filter @light/web exec playwright test e2e/diagnostics-flow.spec.ts` exits 0 (runs clean twice — self-resetting)
  </acceptance_criteria>
  <done>The diagnostics e2e proves the whole DIAG flow: accept → upload → complete → appears on Completed → View opens a signed scan-results URL; DB/bucket left clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query params → server query | from/to/type are attacker-controllable and reach a PostgREST filter builder |
| client → Storage | Result files opened via short-lived signed URLs from the private bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering / Injection | completed page type/date filters | mitigate | `type` validated against a fixed enum whitelist before `.eq`; from/to pass through istRangeFromDates (Date math, NaN-safe) — no raw string interpolation into filters; query is RLS-scoped to lab_tech |
| T-03-09 | Information Disclosure | ResultLink signed URLs | mitigate | Reuses getResultUrl (03-01, T-03-01): 3600s TTL, resolved on click, private bucket — legacy http rows pass through unchanged |
</threat_model>

<verification>
- `pnpm --filter @light/web build` passes.
- `pnpm --filter @light/web exec playwright test e2e/diagnostics-flow.spec.ts` passes and re-runs cleanly.
- Date/type filters change the visible rows via URL query params (server-side).
</verification>

<success_criteria>
- Completed orders list newest-first, filterable by date range and type (server-side), each opening its result via ResultLink.
- The diagnostics e2e covers accept → upload → complete → Completed-page row → signed-URL View, and self-resets.
</success_criteria>

<output>
After completion, create `.planning/phases/03-diagnostics-pharmacy-flow/03-03-SUMMARY.md`
</output>
</content>
