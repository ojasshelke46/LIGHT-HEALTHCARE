---
phase: 02-reception-doctor-flow
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/src/app/doctor/page.tsx
  - apps/web/src/app/doctor/doctor-today.tsx
  - apps/web/src/app/doctor/patient-card.tsx
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "The doctor sees their own checked-in / in-consultation patients for today, live (Realtime), ordered by slot time"
    - "Each patient card shows patient name, age (from dob), IST slot time, status badge, and chief complaint when a visit row exists"
    - "Clicking a card opens that appointment's consultation view"
  artifacts:
    - path: "apps/web/src/app/doctor/doctor-today.tsx"
      provides: "Doctor today live list (useRealtimeList on appointments, scoped to own doctor id)"
      contains: "useRealtimeList"
    - path: "apps/web/src/app/doctor/patient-card.tsx"
      provides: "Patient card: name/age/slot/status/complaint + link to consult"
  key_links:
    - from: "apps/web/src/app/doctor/page.tsx"
      to: "getStaff().id"
      via: "server component reads staff id and passes to client"
      pattern: "getStaff"
    - from: "apps/web/src/app/doctor/doctor-today.tsx"
      to: "appointments filtered doctor_id + today + status in (checked_in, in_consultation)"
      via: "useRealtimeList fetcher"
      pattern: "in\\(\"status\""
---

<objective>
Deliver the doctor's live "today's patients" list: a Realtime board of the signed-in doctor's checked-in and in-consultation appointments for today, each card linking into the consultation view.

Purpose: DOC-01 — the doctor's entry point to the consult step; it must be live so newly checked-in patients appear without refresh.
Output: `/doctor` rendered as a live patient list.
</objective>

## User Story

**As a** doctor, **I want to** see my checked-in patients update live, **so that** I always know who is waiting to be seen next.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-reception-doctor-flow/02-CONTEXT.md
@.planning/phases/02-reception-doctor-flow/02-01-PLAN.md

<interfaces>
From apps/web/src/lib/staff.ts: `getStaff(): Promise<{ id; name; role }>` (server-only). doctor's `doctors.id === staff.id`.
From apps/web/src/lib/hooks/use-realtime.ts: `useRealtimeList<T>(fetcher, tables)`.
From apps/web/src/lib/status.ts: `APPOINTMENT_STATUS_BADGE`. From patient.ts: `ageFromDob`. From format.ts: `formatIST`. From time.ts: `todayISTRange`.
Primitives: `Card`, `Badge`, `Skeleton`, `EmptyState`. Next `<Link>`.
</interfaces>

Doctor-today fetcher (D-26) — own doctor id, today, checked_in/in_consultation:
```typescript
const { startISO, endISO } = todayISTRange();
supabase.from("appointments")
  .select("id, slot_time, status, patient_id, patients(name, dob, phone), visits(chief_complaint)")
  .eq("doctor_id", staffId)
  .gte("slot_time", startISO).lt("slot_time", endISO)
  .in("status", ["checked_in", "in_consultation"])
  .order("slot_time", { ascending: true });
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doctor today server wrapper + live client list (DOC-01)</name>
  <read_first>
    - apps/web/src/app/doctor/page.tsx (current placeholder)
    - apps/web/src/lib/staff.ts (getStaff)
    - apps/web/src/lib/hooks/use-realtime.ts, time.ts, status.ts, patient.ts, format.ts
    - apps/web/src/components/empty-state.tsx, ui/card.tsx
  </read_first>
  <action>
Replace `apps/web/src/app/doctor/page.tsx` with an async **server** component that reads the doctor's own id and hands it to a client child:
```tsx
import { getStaff } from "@/lib/staff";
import { DoctorToday } from "./doctor-today";
export default async function DoctorPage() {
  const staff = await getStaff();
  return <DoctorToday staffId={staff.id} />;
}
```

Create `apps/web/src/app/doctor/doctor-today.tsx` (`"use client"`). Props `{ staffId: string }`:
- Define `TodayRow` type: `{ id; slot_time; status; patient_id; patients: { name; dob; phone } | null; visits: { chief_complaint: string | null }[] }`.
- `useRealtimeList<TodayRow>(fetcher, ["appointments"])` with the D-26 fetcher (memoize fetcher with `useCallback` on `staffId`). Note: also watch `["appointments", "visits"]` so a new visit's chief_complaint appears live — pass both table names.
- Loading → skeleton cards; error → destructive retry (calls `refetch`); empty → `EmptyState` "No patients checked in yet".
- Render each row as `<PatientCard row={row} />` (Task-none; created below in same task) in a responsive grid; sorted by slot_time (already ordered).
- Show a subtle `connected` indicator (aria-live polite).

Create `apps/web/src/app/doctor/patient-card.tsx` (`"use client"`). Props `{ row: TodayRow }` (export the `TodayRow` type from doctor-today or a shared local type; keep it in one place and import):
- `Card` wrapped in a Next `<Link href={"/doctor/consult/" + row.id}>` (`data-testid={"patient-card-" + row.id}`).
- Show patient name (`row.patients?.name ?? "—"`), age (`ageFromDob(row.patients?.dob ?? null)` → "N/A" if null) with " yrs", IST slot time (`formatIST(row.slot_time, "time")`), status `Badge` via `APPOINTMENT_STATUS_BADGE`, and chief complaint (`row.visits?.[0]?.chief_complaint`) when present.
- Keyboard accessible (the Link is focusable); `aria-label` summarizing the card.
  </action>
  <verify>
    <automated>pnpm --filter @light/web typecheck &amp;&amp; pnpm --filter @light/web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "getStaff" apps/web/src/app/doctor/page.tsx`
    - `grep -q "useRealtimeList" apps/web/src/app/doctor/doctor-today.tsx`
    - `grep -Eq "eq\\(\"doctor_id\", staffId\\)" apps/web/src/app/doctor/doctor-today.tsx`
    - `grep -Eq "in\\(\"status\", \\[\"checked_in\", \"in_consultation\"\\]\\)" apps/web/src/app/doctor/doctor-today.tsx`
    - `grep -q "todayISTRange" apps/web/src/app/doctor/doctor-today.tsx`
    - `grep -q "patient-card-" apps/web/src/app/doctor/patient-card.tsx`
    - `grep -q "/doctor/consult/" apps/web/src/app/doctor/patient-card.tsx`
    - `grep -q "ageFromDob" apps/web/src/app/doctor/patient-card.tsx`
    - `grep -q "APPOINTMENT_STATUS_BADGE" apps/web/src/app/doctor/patient-card.tsx`
    - `grep -n ": any" apps/web/src/app/doctor/doctor-today.tsx apps/web/src/app/doctor/patient-card.tsx` returns nothing
    - `pnpm --filter @light/web typecheck` exits 0 and `build` succeeds
  </acceptance_criteria>
  <done>/doctor shows the signed-in doctor's live checked-in/in-consultation list, each card showing name/age/slot/status/complaint and linking to /doctor/consult/[appointmentId].</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor browser → appointments read (scoped) | list must show only THIS doctor's patients |
| patient PII → doctor cards | name/age/complaint rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Information disclosure | doctor today list | mitigate | Fetcher filters `doctor_id = staffId` (from server-verified getStaff) AND RLS scopes doctor reads; a doctor cannot enumerate another doctor's queue |
| T-02-14 | Spoofing | staffId source | mitigate | staffId comes from the server `getStaff()` (getUser revalidation), not from client-controllable input |
</threat_model>

<verification>
- `pnpm --filter @light/web typecheck` + `build` pass.
- Manual/e2e-optional: logging in as `doctor@test.com` shows seeded checked_in (`c0..03`) and in_consultation (`c0..04`) patients; the in_consultation card shows chief complaint "Fever and cough".
</verification>

<success_criteria>
- DOC-01: doctor's own checked-in/in-consultation patients, live, with age/slot/status/complaint, each linking into the consult view.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reception-doctor-flow/02-05-SUMMARY.md`.
</output>
