---
phase: 01-staff-auth-shared-shell
plan: 05
type: execute
wave: 5
depends_on: [03, 04]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, SHELL-01, SHELL-02]
must_haves:
  truths:
    - "A human has confirmed the login → role-home → logout flow works against the live Supabase project"
    - "A human has confirmed the shell is correct on a tablet-width viewport (hamburger drawer, role accent, active nav)"
    - "A human has confirmed cross-role blocking and the loading/error/empty conventions render inside the shell"
  artifacts: []
  key_links:
    - from: "human verification"
      to: "phase success criteria (AUTH-01..03, SHELL-01..02)"
      via: "checkpoint sign-off"
      pattern: "approved"
---

<objective>
Human sign-off on the assembled walking skeleton. All automation (the Playwright
e2e, typecheck, build) already ran in Plans 01-04; this checkpoint is the visual +
functional confirmation of the brief-mandated, tablet-first UI that automated checks
cannot fully judge (correct role accent colors, drawer feel, active-nav highlight,
skeleton/error/empty appearance).

Purpose: Phase 1 is heavily visual and tablet-first is a hard requirement — a human
confirms the shell looks and behaves right before Phase 2 builds on top of it.

Output: A human "approved" (or a defect list to feed the gap-closure loop). No files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-staff-auth-shared-shell/SKELETON.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify login, shell, and async states end-to-end (tablet-first)</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 Success Criteria — the 5 truths being verified)
    - .planning/phases/01-staff-auth-shared-shell/SKELETON.md (capability proven end-to-end)
  </read_first>
  <files>none — verification only, no files modified</files>
  <action>
Start the dev server (`pnpm --filter @light/web dev`, which uses apps/web/.env.local)
and present the running app to the user for manual verification of the seven steps in
&lt;how-to-verify&gt;. Do not modify code in this task — this is a blocking human gate.
If the user reports defects, capture them verbatim as a numbered list (which step,
what happened) so they route into the gap-closure loop; do not attempt fixes here.
  </action>
  <what-built>
A staff login (centered card, zod validation, generic error toast), the existing
role-routing middleware, and a shared DashboardLayout (collapsible sidebar with role
accent + active-nav highlight, mobile hamburger drawer, header with portal name +
staff name from the DB + logout) wrapping all five portals, plus per-portal
loading/error boundaries and a reusable empty state. The Playwright e2e for the login
happy path, cross-role bounce, and invalid-credentials toast is already green.
  </what-built>
  <how-to-verify>
Prerequisite: dev server running — `pnpm --filter @light/web dev` (uses apps/web/.env.local).
Use a seeded staff account (confirm via Supabase if the documented default
`doctor@test.com` / `Test1234!` does not work).

1. LOGIN + REDIRECT (AUTH-01, AUTH-02): Visit http://localhost:3000/login. Submit a
   malformed email → expect an inline validation message, no spinner. Submit valid
   credentials → expect redirect to the role home (e.g. /doctor).
2. ERROR TOAST (AUTH-01): Log out, then submit valid email + wrong password → expect a
   toast reading "Invalid email or password" (NOT raw error text), staying on /login.
3. SHELL (SHELL-01): On the role home, confirm the header shows the portal name, YOUR
   staff name, and a logout control; the sidebar uses the role accent color (reception
   blue, doctor purple, diagnostics amber, pharmacy green, admin gray) and highlights
   the active nav item.
4. TABLET / MOBILE (SHELL-01, brief tablet-first): Narrow the window below ~768px (or
   use device emulation). Confirm the sidebar collapses and a hamburger opens it as an
   overlay drawer; the drawer closes on nav tap, overlay tap, and Escape. On desktop,
   confirm the collapse-to-icon-rail toggle works.
5. LOGOUT (AUTH-03): Click logout in the header → expect return to /login.
6. CROSS-ROLE BLOCK (AUTH-02): Log back in, then manually visit another role's URL
   (e.g. as doctor go to /pharmacy) → expect a bounce back to your own home.
7. ASYNC STATES (SHELL-02): Throttle the network (DevTools "Slow 3G") and reload a
   portal → expect a page-shaped skeleton before content. (Error/empty states are
   established components; confirm the skeleton renders inside the shell chrome.)
  </how-to-verify>
  <verify>Human types "approved" after confirming all 7 steps, or provides a numbered defect list.</verify>
  <acceptance_criteria>
    - Steps 1-7 each behave as described; no raw error text is shown to the user anywhere
    - Role accent colors match the brief exactly (blue/purple/amber/green/gray)
    - The hamburger drawer works below md and closes on nav/overlay/Escape
    - Logout returns to /login; cross-role navigation bounces to own home
    - The loading skeleton renders inside the DashboardLayout chrome (not a bare page)
  </acceptance_criteria>
  <done>User has typed "approved" (phase success criteria confirmed) OR a concrete defect list has been captured for gap closure.</done>
  <resume-signal>Type "approved" to complete Phase 1, or list the specific defects (which step, what happened) to route into the gap-closure loop.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human tester → live Supabase | Manual login exercises the real auth + session + role-routing path end to end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Elevation of privilege | cross-role access (manual) | mitigate (verify) | Step 6 manually confirms the middleware bounce that T-01-08 mitigates; failure here blocks the phase |
| T-01-16 | Information disclosure | error surfaces (manual) | mitigate (verify) | Steps 2 + 7 confirm no raw error text/stack reaches the user (T-01-04, T-01-13) |
</threat_model>

<verification>
- Human confirms all 7 steps; the automated e2e/typecheck/build gates from Plans 01-04 are already green.
</verification>

<success_criteria>
- All five Phase-1 ROADMAP success criteria are confirmed by a human against the live app.
- Any defects are captured as a concrete list for gap closure rather than silently passing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-staff-auth-shared-shell/01-05-SUMMARY.md`
</output>
