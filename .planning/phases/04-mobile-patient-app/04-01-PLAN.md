---
phase: 04-mobile-patient-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/metro.config.js
  - apps/mobile/babel.config.js
  - apps/mobile/tsconfig.json
  - apps/mobile/tailwind.config.js
  - apps/mobile/global.css
  - apps/mobile/nativewind-env.d.ts
  - apps/mobile/.env
  - apps/mobile/.gitignore
  - apps/mobile/vitest.config.ts
  - apps/mobile/lib/supabase.ts
  - apps/mobile/lib/format.ts
  - apps/mobile/lib/status.ts
  - apps/mobile/lib/phone.ts
  - apps/mobile/lib/phone.test.ts
  - apps/mobile/lib/session.tsx
  - apps/mobile/components/ui.tsx
  - apps/mobile/components/AppointmentCard.tsx
  - apps/mobile/components/QRView.tsx
  - apps/mobile/app/_layout.tsx
  - "apps/mobile/app/(auth)/_layout.tsx"
  - "apps/mobile/app/(auth)/login.tsx"
  - "apps/mobile/app/(auth)/complete-profile.tsx"
  - "apps/mobile/app/(tabs)/_layout.tsx"
  - "apps/mobile/app/(tabs)/index.tsx"
  - "apps/mobile/app/(tabs)/book.tsx"
  - "apps/mobile/app/(tabs)/appointments.tsx"
  - "apps/mobile/app/(tabs)/reports.tsx"
  - "apps/mobile/app/(tabs)/profile.tsx"
autonomous: true
requirements: [AUTH-04]
user_setup:
  - service: supabase-auth-phone
    why: "Real phone-OTP sign-in (D-48 production path). Coded but untestable until an SMS provider is configured."
    dashboard_config:
      - task: "Enable the Phone provider and attach an SMS provider (e.g. Twilio) so signInWithOtp({ phone }) delivers codes"
        location: "Supabase Dashboard -> Authentication -> Providers -> Phone"
    note: "Until configured, use the __DEV__ Dev-login (patient@test.com / Test1234!). The OTP code path is fully implemented."

must_haves:
  truths:
    - "The Expo app boots: `expo export --platform ios` produces a JS bundle with no errors"
    - "An unauthenticated user is routed to the phone-OTP login screen"
    - "In development, a Dev-login (email/password) signs the seeded patient in"
    - "On first sign-in without a patient row, the user is prompted to complete their profile, which inserts a patients row bound to their auth uid"
    - "Session persists across app restarts via the AsyncStorage auth adapter"
  artifacts:
    - path: "apps/mobile/metro.config.js"
      provides: "pnpm-monorepo Metro config + NativeWind transform"
      contains: "watchFolders"
    - path: "apps/mobile/lib/supabase.ts"
      provides: "Typed RN Supabase client with AsyncStorage session storage"
      contains: "react-native-url-polyfill/auto"
    - path: "apps/mobile/lib/session.tsx"
      provides: "AuthProvider: session + patient row + auth-state listener"
      contains: "onAuthStateChange"
    - path: "apps/mobile/app/_layout.tsx"
      provides: "Root route gate (session + patient row)"
      contains: "Redirect"
    - path: "apps/mobile/app/(auth)/login.tsx"
      provides: "Phone-OTP login + __DEV__ dev-login"
      contains: "signInWithOtp"
    - path: "apps/mobile/app/(auth)/complete-profile.tsx"
      provides: "First-run patient-row creation"
      contains: "insert"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/lib/session.tsx"
      via: "AuthProvider + useSession gate"
      pattern: "useSession|AuthProvider"
    - from: "apps/mobile/app/(auth)/login.tsx"
      to: "apps/mobile/lib/supabase.ts"
      via: "supabase.auth OTP + dev password"
      pattern: "supabase\\.auth"
    - from: "apps/mobile/lib/supabase.ts"
      to: "@light/shared-types"
      via: "createClient<Database>"
      pattern: "createClient<Database>"
---

<objective>
Scaffold the `@light/mobile` Expo app inside the pnpm monorepo and deliver the authentication foundation: a patient signs in (phone OTP in production, __DEV__ email/password in development), a patient row is auto-created on first sign-in, the session persists, and the root layout gates the tab shell on `session + patient row`. This is the vertical slice that proves the full mobile stack compiles and a patient can get *into* the app.

Purpose: Everything in Phase 4 (home, booking, appointments, reports, profile) depends on a booting app, a typed Supabase client, an IST/format/status/UI kit, and a live patient session. This plan builds all of it. (AUTH-04)
Output: A bootable Expo app (`expo export` succeeds), shared lib + UI kit, and working auth + patient-row provisioning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mobile-patient-app/04-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Backend contracts + web patterns to MIRROR (do not re-invent):
@packages/shared-types/src/database.types.ts
@apps/web/src/lib/supabase/client.ts
@apps/web/src/lib/format.ts
@apps/web/src/lib/status.ts

<interfaces>
<!-- Executor uses these directly — no codebase exploration needed. -->

@light/shared-types exports:
```ts
export type { Database } from "./database.types";
export type AppointmentStatus = Database["public"]["Enums"]["appointment_status"];
// = "booked" | "checked_in" | "in_consultation" | "completed" | "no_show" | "cancelled"
```

patients Row (the auth target):
```ts
type Patient = {
  id: string; name: string; phone: string;
  email: string | null; dob: string | null; address: string | null;
  abha_id: string | null; auth_user_id: string | null; created_at: string | null;
};
```

Env: values already exist in apps/web/.env.local as NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY.
Seeded dev patient: patient@test.com / Test1234!  ->  patients row a0..01 (Aarav Sharma).
Deployed + verified: patients_self_insert RLS (auth.uid() ownership), current_patient_id() helper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Expo SDK 52 app + all monorepo config, then install</name>
  <files>apps/mobile/package.json, apps/mobile/app.json, apps/mobile/metro.config.js, apps/mobile/babel.config.js, apps/mobile/tsconfig.json, apps/mobile/tailwind.config.js, apps/mobile/global.css, apps/mobile/nativewind-env.d.ts, apps/mobile/.env, apps/mobile/.gitignore, apps/mobile/vitest.config.ts</files>
  <read_first>
    - apps/mobile/README.md (placeholder — overwrite the dir from scratch)
    - package.json (root: packageManager pnpm@10.33.0, engines node>=20)
    - pnpm-workspace.yaml (apps/* already a member)
    - tsconfig.base.json (repo base — mobile does NOT extend this; it extends expo/tsconfig.base)
  </read_first>
  <action>
Create the app skeleton with EXACT file contents below. This is a manual scaffold (D-45/D-46) — do NOT run `create-expo-app`.

**apps/mobile/package.json** (versions are a starting point; Task reconciles them with `expo install --fix`):
```json
{
  "name": "@light/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "export": "expo export --platform ios"
  },
  "dependencies": {
    "@light/shared-types": "workspace:*",
    "@react-native-async-storage/async-storage": "1.23.1",
    "@supabase/supabase-js": "^2.47.10",
    "date-fns": "^4.1.0",
    "expo": "~52.0.0",
    "expo-constants": "~17.0.0",
    "expo-linking": "~7.0.0",
    "expo-router": "~4.0.0",
    "expo-status-bar": "~2.0.0",
    "nativewind": "^4.1.23",
    "react": "18.3.1",
    "react-native": "0.76.5",
    "react-native-gesture-handler": "~2.20.2",
    "react-native-qrcode-svg": "^6.3.14",
    "react-native-reanimated": "~3.16.1",
    "react-native-safe-area-context": "4.12.0",
    "react-native-screens": "~4.4.0",
    "react-native-svg": "15.8.0",
    "react-native-url-polyfill": "^2.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@types/react": "~18.3.12",
    "tailwindcss": "^3.4.17",
    "typescript": "~5.3.3",
    "vitest": "^2.1.8"
  }
}
```

**apps/mobile/app.json**:
```json
{
  "expo": {
    "name": "Light Patient",
    "slug": "light-patient",
    "scheme": "lightpatient",
    "version": "1.0.0",
    "orientation": "portrait",
    "userInterfaceStyle": "light",
    "newArchEnabled": true,
    "ios": { "supportsTablet": true, "bundleIdentifier": "com.light.patient" },
    "android": { "package": "com.light.patient" },
    "web": { "bundler": "metro" },
    "plugins": ["expo-router"],
    "experiments": { "typedRoutes": true }
  }
}
```

**apps/mobile/metro.config.js** (pnpm-monorepo recipe, D-46 — REQUIRED exact contents):
```js
// Expo + pnpm monorepo Metro config (D-46). watchFolders = repo root so Metro
// can resolve the hoisted workspace deps and @light/shared-types; NativeWind
// transform wraps the default config.
const { getDefaultConfig } = require("expo/metro-config");
const { withNativeWind } = require("nativewind/metro");
const path = require("path");

const projectRoot = __dirname;
const workspaceRoot = path.resolve(projectRoot, "../..");

const config = getDefaultConfig(projectRoot);
config.watchFolders = [workspaceRoot];
config.resolver.nodeModulesPaths = [
  path.resolve(projectRoot, "node_modules"),
  path.resolve(workspaceRoot, "node_modules"),
];
config.resolver.disableHierarchicalLookup = true;

module.exports = withNativeWind(config, { input: "./global.css" });
```

**apps/mobile/babel.config.js** (NativeWind v4 + Expo SDK 52 — REQUIRED exact contents):
```js
module.exports = function (api) {
  api.cache(true);
  return {
    presets: [
      ["babel-preset-expo", { jsxImportSource: "nativewind" }],
      "nativewind/babel",
    ],
  };
};
```

**apps/mobile/tailwind.config.js** (NativeWind v4 preset + teal patient accent D-55 — REQUIRED exact contents):
```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./app/**/*.{js,jsx,ts,tsx}", "./components/**/*.{js,jsx,ts,tsx}"],
  presets: [require("nativewind/preset")],
  theme: {
    extend: {
      colors: {
        brand: { DEFAULT: "#0d9488", dark: "#0f766e", light: "#ccfbf1" },
      },
    },
  },
  plugins: [],
};
```

**apps/mobile/global.css**:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

**apps/mobile/nativewind-env.d.ts**:
```ts
/// <reference types="nativewind/types" />
```

**apps/mobile/tsconfig.json**:
```json
{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": { "@/*": ["./*"] }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts",
    "nativewind-env.d.ts"
  ]
}
```

**apps/mobile/.gitignore**:
```
node_modules/
.expo/
dist/
*.log
.env
```

**apps/mobile/.env** — copy the two values from apps/web/.env.local (same Supabase project). Read `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` out of apps/web/.env.local and write them under the EXPO_PUBLIC_ prefix:
```
EXPO_PUBLIC_SUPABASE_URL=<value of NEXT_PUBLIC_SUPABASE_URL from apps/web/.env.local>
EXPO_PUBLIC_SUPABASE_ANON_KEY=<value of NEXT_PUBLIC_SUPABASE_ANON_KEY from apps/web/.env.local>
```

**apps/mobile/vitest.config.ts** (pure-logic unit tests only — node env, no RN):
```ts
import { defineConfig } from "vitest/config";
export default defineConfig({
  test: { environment: "node", include: ["lib/**/*.test.ts"] },
});
```

Then install (single consolidated install — this is the slow step, do it once):
```
pnpm install
cd apps/mobile && npx expo install --fix
```
`expo install --fix` is the AUTHORITY on versions — it rewrites any SDK-52-incompatible pins in package.json. Re-run `pnpm install` if it changes package.json. Do NOT hand-tune versions after this step.

Note: a temporary placeholder `apps/mobile/app/index.tsx` is NOT needed — Task 2 creates the real route tree. If `expo export` in this task's verify needs a route, add a one-line `app/index.tsx` that re-exports from expo-router's default; Task 2 overwrites the whole `app/` tree.
  </action>
  <acceptance_criteria>
    - `test -f apps/mobile/metro.config.js && grep -q "watchFolders" apps/mobile/metro.config.js`
    - `grep -q "nativewind/preset" apps/mobile/tailwind.config.js`
    - `grep -q "jsxImportSource" apps/mobile/babel.config.js`
    - `grep -q "EXPO_PUBLIC_SUPABASE_URL=." apps/mobile/.env` (value is non-empty, copied from web)
    - `test -d apps/mobile/node_modules/expo` (install succeeded)
    - `pnpm --filter @light/mobile exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>All config files exist with the exact contents above; deps installed; `expo install --fix` has reconciled versions; tsc passes on the scaffold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supabase client + IST/status/UI kit + session gate + tab shell</name>
  <files>apps/mobile/lib/supabase.ts, apps/mobile/lib/format.ts, apps/mobile/lib/status.ts, apps/mobile/lib/phone.ts, apps/mobile/lib/phone.test.ts, apps/mobile/lib/session.tsx, apps/mobile/components/ui.tsx, apps/mobile/components/AppointmentCard.tsx, apps/mobile/components/QRView.tsx, apps/mobile/app/_layout.tsx, apps/mobile/app/(tabs)/_layout.tsx, apps/mobile/app/(tabs)/index.tsx, apps/mobile/app/(tabs)/book.tsx, apps/mobile/app/(tabs)/appointments.tsx, apps/mobile/app/(tabs)/reports.tsx, apps/mobile/app/(tabs)/profile.tsx</files>
  <read_first>
    - apps/web/src/lib/supabase/client.ts (createBrowserClient<Database> pattern to mirror)
    - apps/web/src/lib/format.ts (formatIST — reimplement, Intl is available in Hermes)
    - apps/web/src/lib/status.ts (APPOINTMENT_STATUS_BADGE map — reimplement with RN className strings)
    - packages/shared-types/src/database.types.ts (patients Row, AppointmentStatus enum)
  </read_first>
  <behavior>
    - lib/phone.ts `normalizePhone("98765 43210")` -> `"+919876543210"`; already-prefixed `"+919876543210"` -> unchanged; rejects non-10-digit local numbers (returns null).
    - lib/status.ts `APPOINTMENT_STATUS_BADGE.booked` -> `{ label: "Booked", className: "bg-slate-100 text-slate-700" }` for all 6 enum values (RN/NativeWind class strings).
    - lib/format.ts `formatIST(iso, "time")` renders Asia/Kolkata hh:mm; invalid input -> "—".
  </behavior>
  <action>
Create the shared foundation. Keep tested logic in PURE `.ts` files (no react-native imports) so vitest runs them in node.

**apps/mobile/lib/supabase.ts** (RN client — REQUIRED exact shape, D-46):
```ts
import "react-native-url-polyfill/auto";
import AsyncStorage from "@react-native-async-storage/async-storage";
import { createClient } from "@supabase/supabase-js";
import type { Database } from "@light/shared-types";

const url = process.env.EXPO_PUBLIC_SUPABASE_URL!;
const anonKey = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!;

export const supabase = createClient<Database>(url, anonKey, {
  auth: {
    storage: AsyncStorage,
    autoRefreshToken: true,
    persistSession: true,
    detectSessionInUrl: false,
  },
});
```

**apps/mobile/lib/format.ts** — reimplement `formatIST(input, style)` exactly mirroring apps/web/src/lib/format.ts (const IST = "Asia/Kolkata", Intl.DateTimeFormat("en-IN"), invalid -> "—"). No web imports (D-55).

**apps/mobile/lib/status.ts** — export `APPOINTMENT_STATUS_BADGE: Record<AppointmentStatus, { label: string; className: string }>` mirroring web's map (booked slate, checked_in blue, in_consultation yellow, completed green, no_show red, cancelled bordered). NativeWind supports these Tailwind class strings. Import `AppointmentStatus` from `@light/shared-types`.

**apps/mobile/lib/phone.ts** — pure `normalizePhone(raw: string): string | null` (+91 default, strips spaces/dashes, validates 10 local digits per D-48). Pure — no RN import.

**apps/mobile/lib/phone.test.ts** — vitest: valid 10-digit -> "+91…", spaced input normalized, already-prefixed unchanged, 9-digit -> null, letters -> null.

**apps/mobile/components/ui.tsx** — small NativeWind primitives used app-wide (D-55): `Screen` (SafeAreaView + padded View), `Skeleton` (pulsing bg-slate-200 view), `EmptyState({ title, hint })`, `ErrorState({ message, onRetry })`, `Button({ title, onPress, loading, variant })` (brand teal), `Field` (labeled TextInput). All use `className`. Add `aria`/accessibilityLabel + accessibilityRole on Button and Field per CLAUDE.md a11y.

**apps/mobile/components/QRView.tsx** — wraps `react-native-qrcode-svg`: `QRView({ value, size = 200 })` renders `<QRCode value={value} size={size} />` centered. (Shared: booking success + appointment detail modal.)

**apps/mobile/components/AppointmentCard.tsx** — presentational: props `{ doctorName, deptName, slotTimeISO, status, onPress? }`; renders doctor/dept, `formatIST(slotTimeISO, "datetime")`, and a status pill from `APPOINTMENT_STATUS_BADGE[status]`. (Shared: home + appointments.)

**apps/mobile/lib/session.tsx** — `AuthProvider` + `useSession()` context (D-49). State: `{ session, patient, loading }`. On mount: `supabase.auth.getSession()`; subscribe `supabase.auth.onAuthStateChange`. Whenever a session exists, resolve the patient row: `supabase.from("patients").select("*").eq("auth_user_id", session.user.id).maybeSingle()`. Expose `patient` (or null when the row is missing -> triggers complete-profile) and `refreshPatient()`. Prefer context (no zustand needed per D-45).

**apps/mobile/app/_layout.tsx** — root: wrap tree in `<AuthProvider>`; a gate component reads `useSession()`:
  - `loading` -> full-screen Skeleton splash
  - no `session` -> `<Redirect href="/(auth)/login" />`
  - `session` but no `patient` -> `<Redirect href="/(auth)/complete-profile" />`
  - else render `<Stack>` (expo-router) exposing the (auth) and (tabs) groups.

**apps/mobile/app/(auth)/_layout.tsx** — `<Stack screenOptions={{ headerShown: false }} />`.

**apps/mobile/app/(tabs)/_layout.tsx** — `<Tabs>` with 5 screens in this order: `index` (Home), `book` (Book), `appointments` (Appointments), `reports` (Reports), `profile` (Profile). Teal `tabBarActiveTintColor: "#0d9488"`. Choose tab icons from @expo/vector-icons or lucide-react-native (Claude's discretion per D-55).

**apps/mobile/app/(tabs)/index.tsx, book.tsx, appointments.tsx, reports.tsx, profile.tsx** — PLACEHOLDER screens for now: each a `Screen` with an `EmptyState` naming the tab. Plans 02/03/04 overwrite index/book, appointments/reports, and profile respectively.

Leave `app/(auth)/login.tsx` and `complete-profile.tsx` to Task 3.
  </action>
  <acceptance_criteria>
    - `grep -q "react-native-url-polyfill/auto" apps/mobile/lib/supabase.ts && grep -q "storage: AsyncStorage" apps/mobile/lib/supabase.ts`
    - `grep -q "createClient<Database>" apps/mobile/lib/supabase.ts`
    - `grep -q "Asia/Kolkata" apps/mobile/lib/format.ts`
    - `grep -q "onAuthStateChange" apps/mobile/lib/session.tsx`
    - `grep -q "Redirect" apps/mobile/app/_layout.tsx`
    - `grep -Eq "QRCode" apps/mobile/components/QRView.tsx`
    - `pnpm --filter @light/mobile test` passes (phone.test.ts)
    - `pnpm --filter @light/mobile exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Typed RN Supabase client, IST/status/phone libs, UI kit + shared QRView/AppointmentCard, AuthProvider gate, and the 5-tab placeholder shell all compile; phone unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Auth screen (phone OTP + __DEV__ dev-login) + first-run patient row</name>
  <files>apps/mobile/app/(auth)/_layout.tsx, apps/mobile/app/(auth)/login.tsx, apps/mobile/app/(auth)/complete-profile.tsx</files>
  <read_first>
    - apps/mobile/lib/session.tsx (Task 2 — useSession/refreshPatient)
    - apps/mobile/lib/phone.ts (Task 2 — normalizePhone)
    - apps/mobile/lib/supabase.ts (Task 2 — supabase.auth)
    - apps/web/src/app/login/page.tsx (zod + error-toast login pattern to mirror in RN)
  </read_first>
  <behavior>
    - Phone step: valid normalized phone enables "Send code"; invalid -> inline zod error, button disabled.
    - After signInWithOtp, UI advances to a 6-digit code step; verifyOtp on submit.
    - __DEV__ dev-login: collapsible that calls signInWithPassword(patient@test.com/Test1234!); the whole block is inside `if (__DEV__)` so it is dead-code-eliminated from production bundles.
    - complete-profile: name required (zod), phone prefilled read-only from auth.user.phone; submit inserts a patients row {name, phone, auth_user_id = uid} then refreshPatient().
  </behavior>
  <action>
**apps/mobile/app/(auth)/login.tsx** (D-48, AUTH-04):
  - Staged state: `"phone" | "code"`.
  - Phone step: `Field` for the 10-digit number (prefix +91 shown), zod `z.string()` validated via `normalizePhone` (null -> inline error). "Send code" -> `supabase.auth.signInWithOtp({ phone: normalized })`; on success advance to `"code"`; on error show a toast/Alert (no raw error text — friendly message).
  - Code step: 6-digit `Field` -> `supabase.auth.verifyOtp({ phone: normalized, token, type: "sms" })`. Success: the AuthProvider listener handles routing (do NOT manually navigate). Error -> Alert.
  - **Dev-login** — wrap in `if (__DEV__) { … }`: a collapsible "Dev login" with email/password Fields defaulting to `patient@test.com` / `Test1234!`, button calls `supabase.auth.signInWithPassword({ email, password })`. This block MUST be inside the `__DEV__` guard (T-04-03).
  - Use `Alert.alert` or a lightweight toast for errors; loading state on buttons.

**apps/mobile/app/(auth)/complete-profile.tsx** (D-49, first-run patient row):
  - zod schema `{ name: z.string().min(1) }`; phone prefilled from `useSession().session.user.phone` (or user.email for the dev path), rendered read-only.
  - On submit: `supabase.from("patients").insert({ name, phone: authPhone, auth_user_id: session.user.id })` — patients_self_insert RLS enforces `auth.uid()` ownership server-side (DEPLOYED). On success call `refreshPatient()`; the root gate then routes to (tabs).
  - Handle the dev-login seeded patient: patient@test.com is ALREADY linked to patients a0..01, so `refreshPatient()` will find a row and the gate skips complete-profile. This screen only fires for brand-new OTP users. Guard the insert against a duplicate (if a row already exists, just refreshPatient).
  - loading/error states; friendly error on insert failure.

Do NOT add navigation calls after auth — routing is centralized in the root gate (Task 2).
  </action>
  <acceptance_criteria>
    - `grep -q "signInWithOtp" "apps/mobile/app/(auth)/login.tsx"`
    - `grep -q "verifyOtp" "apps/mobile/app/(auth)/login.tsx"`
    - `grep -q "__DEV__" "apps/mobile/app/(auth)/login.tsx"` (dev-login guarded)
    - `grep -q "signInWithPassword" "apps/mobile/app/(auth)/login.tsx"`
    - `grep -Eq "from\\(\"patients\"\\)\\.insert|insert\\(" "apps/mobile/app/(auth)/complete-profile.tsx"`
    - `grep -q "auth_user_id" "apps/mobile/app/(auth)/complete-profile.tsx"`
    - `pnpm --filter @light/mobile exec tsc --noEmit` exits 0
    - `pnpm --filter @light/mobile run export` exits 0 (bundle compiles end-to-end)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx expo export --platform ios</automated>
  </verify>
  <done>Login screen offers phone-OTP (production path) + __DEV__ dev-login; complete-profile inserts an auth-bound patient row; the full app bundles via `expo export`. AUTH-04 satisfied (verify real OTP delivery once SMS provider configured — see user_setup).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device -> Supabase Auth | Untrusted phone number + OTP/password cross to Supabase Auth |
| device storage | Session token persisted on-device via AsyncStorage |
| device -> patients INSERT | First-run profile write crosses into the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Info disclosure) | lib/supabase.ts AsyncStorage session | accept | AsyncStorage is app-sandboxed; token is a short-lived auto-refreshed JWT; anon key is public by design. Documented SecureStore (expo-secure-store) upgrade path for v2. |
| T-04-02 | S (Spoofing) | (auth)/login.tsx verifyOtp | transfer | OTP attempt throttling is enforced server-side by Supabase Auth rate limits; client shows a generic error and keeps NO local attempt counter to game. |
| T-04-03 | E (Elevation) | (auth)/login.tsx dev-login | mitigate | Dev-login is wrapped in `if (__DEV__)`; Hermes/Metro dead-code-eliminates it from production release bundles. Grep gate asserts the `__DEV__` guard exists. |
| T-04-04 | T (Tampering) | (auth)/complete-profile.tsx insert | mitigate | Insert sets `auth_user_id = session.user.id`; patients_self_insert RLS enforces `auth.uid()` ownership server-side, so a forged id is rejected by Postgres, not trusted from the client. |
</threat_model>

<verification>
- `pnpm --filter @light/mobile exec tsc --noEmit` exits 0 (strict types, no `any`, typed Supabase).
- `pnpm --filter @light/mobile run export` (== `expo export --platform ios`) exits 0 — the whole route tree bundles.
- `pnpm --filter @light/mobile test` (vitest) passes — phone normalization.
- Grep gates in each task's acceptance_criteria pass.
</verification>

<success_criteria>
- App boots to a gated root: unauthenticated -> login; authenticated-without-row -> complete-profile; authenticated-with-row -> tabs.
- Phone-OTP path is fully coded (production); __DEV__ dev-login works against the seeded patient.
- A new sign-in provisions an auth-bound patients row (patients_self_insert).
- Session survives restart (AsyncStorage adapter).
- AUTH-04 satisfied in code (real OTP requires the SMS provider from user_setup).
</success_criteria>

<output>
After completion, create `.planning/phases/04-mobile-patient-app/04-01-SUMMARY.md`.
Document: (1) the SMS-provider user action needed for real OTP; (2) the exact versions `expo install --fix` settled on; (3) any deviation from the pinned deps.
</output>
