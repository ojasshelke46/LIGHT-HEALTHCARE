---
phase: 04-mobile-patient-app
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - "apps/mobile/app/(tabs)/profile.tsx"
  - apps/mobile/lib/profileSchema.ts
  - apps/mobile/lib/profileSchema.test.ts
autonomous: true
requirements: [MOB-05]

must_haves:
  truths:
    - "Profile shows the patient's name, phone (read-only), email, address, and ABHA id"
    - "Patient can edit name/email/address/ABHA with zod validation and save"
    - "Phone cannot be edited (auth identity)"
    - "A successful save updates the patients row and reflects in the session"
  artifacts:
    - path: "apps/mobile/app/(tabs)/profile.tsx"
      provides: "Profile view + edit form + save"
      contains: "update"
    - path: "apps/mobile/lib/profileSchema.ts"
      provides: "zod schema for editable patient fields"
      exports: ["profileSchema"]
  key_links:
    - from: "apps/mobile/app/(tabs)/profile.tsx"
      to: "patients table"
      via: "self-update scoped to own id"
      pattern: "from\\(\"patients\"\\)\\.update"
    - from: "apps/mobile/app/(tabs)/profile.tsx"
      to: "apps/mobile/lib/profileSchema.ts"
      via: "zod parse before save"
      pattern: "profileSchema"
---

<objective>
Deliver the Profile tab: view the patient's details (name, phone read-only, email, address, ABHA id) and edit name/email/address/ABHA with zod validation, saving via a self-scoped patients update.

Purpose: MOB-05. The patient owns their demographic record incl. the ABHA id (text field per project scope). Phone is the auth identity and stays read-only.
Output: Profile view/edit vertical slice reusing the UI kit + session from Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mobile-patient-app/04-CONTEXT.md
@.planning/phases/04-mobile-patient-app/04-01-SUMMARY.md

# Foundation from Plan 01 (consume, do not modify):
@apps/mobile/lib/supabase.ts
@apps/mobile/lib/session.tsx
@apps/mobile/components/ui.tsx

<interfaces>
<!-- patients Row (from packages/shared-types/src/database.types.ts). Editable subset per D-54. -->
```ts
type Patient = {
  id: string; name: string; phone: string;   // phone = auth identity, READ-ONLY
  email: string | null; address: string | null; abha_id: string | null;
  dob: string | null; auth_user_id: string | null;
};
```
Session: `useSession()` from lib/session.tsx exposes `{ patient, refreshPatient }` (patient row + a refresher).

BACKEND PRECONDITION (verify + document, do NOT create a migration): editing requires a
`patients_self_update` RLS policy (auth.uid() = auth_user_id). It is NOT in the orchestrator's
confirmed-deployed list (which covers patients_self_insert / departments_public_select /
book_appointment). Attempt the update; if it returns an RLS error (403 / "new row violates
row-level security" / 42501), surface a friendly message AND record it in the SUMMARY as a
backend gap for the orchestrator to deploy — matching the Phase-01/02 precedent for backend
preconditions. Do not silently drop the feature.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: profileSchema (zod) for editable patient fields</name>
  <files>apps/mobile/lib/profileSchema.ts, apps/mobile/lib/profileSchema.test.ts</files>
  <read_first>
    - packages/shared-types/src/database.types.ts (patients Row — editable subset)
    - apps/web/src/app/login/page.tsx (project zod usage convention)
  </read_first>
  <behavior>
    - profileSchema requires `name` (min 1); `email` optional but must be a valid email when non-empty; `address` and `abha_id` optional strings.
    - parsing `{ name: "" }` fails; `{ name: "Aarav" }` passes; `{ name: "A", email: "nope" }` fails; `{ name: "A", email: "" }` passes (empty allowed); `{ name: "A", abha_id: "12-3456" }` passes.
    - phone is NOT a field of the schema (read-only, excluded).
  </behavior>
  <action>
**apps/mobile/lib/profileSchema.ts** — PURE (zod only, no RN import):
  ```ts
  import { z } from "zod";
  export const profileSchema = z.object({
    name: z.string().trim().min(1, "Name is required"),
    email: z.string().trim().email("Enter a valid email").or(z.literal("")).optional(),
    address: z.string().trim().optional(),
    abha_id: z.string().trim().optional(),
  });
  export type ProfileInput = z.infer<typeof profileSchema>;
  ```
  (Match the web convention; note STATE.md logged that zod v4.4.3's `.email()` alias still works. `phone` is deliberately absent — it is read-only.)

**apps/mobile/lib/profileSchema.test.ts** — vitest cases from `<behavior>`.
  </action>
  <acceptance_criteria>
    - `grep -q "z.object" apps/mobile/lib/profileSchema.ts`
    - `grep -q "abha_id" apps/mobile/lib/profileSchema.ts`
    - `grep -vq "phone" apps/mobile/lib/profileSchema.ts` (phone excluded — read-only)
    - `grep -vq "react-native" apps/mobile/lib/profileSchema.ts` (stays pure)
    - `pnpm --filter @light/mobile test` passes (profileSchema.test.ts)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>zod schema validates the editable patient subset (name/email/address/abha), excludes phone, and is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Profile tab — view + edit + self-update (MOB-05)</name>
  <files>apps/mobile/app/(tabs)/profile.tsx</files>
  <read_first>
    - apps/mobile/lib/profileSchema.ts (Task 1)
    - apps/mobile/lib/session.tsx (useSession -> patient, refreshPatient)
    - apps/mobile/components/ui.tsx (Field, Button, Screen, ErrorState)
  </read_first>
  <action>
Overwrite the placeholder **apps/mobile/app/(tabs)/profile.tsx** (D-54, MOB-05):
  - Read the current patient from `useSession().patient`; initialize form state from it.
  - `Field`s for name, email, address, abha_id (editable) + a read-only display for phone (rendered but not a form input; label it "Phone (verified)"). dob shown read-only if present.
  - On Save:
    ```ts
    const parsed = profileSchema.safeParse(form);
    if (!parsed.success) { /* show inline field errors */ return; }
    const { error } = await supabase.from("patients")
      .update({
        name: parsed.data.name,
        email: parsed.data.email || null,
        address: parsed.data.address || null,
        abha_id: parsed.data.abha_id || null,
      })
      .eq("id", patient.id);   // own id only; RLS also enforces ownership
    ```
    - NEVER include `phone`, `id`, or `auth_user_id` in the update payload (T-04-11 / T-04-12).
    - Success: toast "Profile saved" + `refreshPatient()`.
    - Error: if it looks like an RLS denial (403 / 42501 / "row-level security"), show "Couldn't save — profile updates aren't enabled yet" and log to console; otherwise a generic friendly error. (Backend precondition — see `<interfaces>`; document the outcome in the SUMMARY.)
  - Loading state on Save; disabled while in-flight. Accessibility labels on all fields + the Save button.
  </action>
  <acceptance_criteria>
    - `grep -q "from(\"patients\").update" "apps/mobile/app/(tabs)/profile.tsx"`
    - `grep -q "profileSchema" "apps/mobile/app/(tabs)/profile.tsx"`
    - `grep -q "abha_id" "apps/mobile/app/(tabs)/profile.tsx"`
    - `grep -Eq "read-?only|editable={false}|Phone \\(verified\\)" "apps/mobile/app/(tabs)/profile.tsx"` (phone not editable)
    - `grep -q "refreshPatient" "apps/mobile/app/(tabs)/profile.tsx"`
    - `pnpm --filter @light/mobile exec tsc --noEmit` exits 0
    - `pnpm --filter @light/mobile run export` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx expo export --platform ios</automated>
  </verify>
  <done>Profile shows patient details (phone read-only), edits name/email/address/ABHA with zod, and saves via a self-scoped patients update reflected in the session. MOB-05 satisfied; bundle compiles. Any patients_self_update RLS gap documented in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patient client -> patients UPDATE | Demographic edits cross into the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | T (Tampering) | profile.tsx update payload | mitigate | Update is filtered `.eq("id", ownPatientId)` and the payload contains ONLY name/email/address/abha_id — never id or auth_user_id; patients_self_update RLS (auth.uid() = auth_user_id) enforces ownership server-side so another patient's row cannot be touched. |
| T-04-12 | T (Tampering) | profile.tsx phone field | mitigate | Phone is the auth identity: rendered read-only, excluded from the zod schema AND the update payload, so it cannot be changed from the client. |
| T-04-13 | I (Info disclosure) | useSession patient in form | accept | Only the signed-in patient's own row (RLS-scoped) populates the form; no cross-patient data reachable. |
</threat_model>

<verification>
- `pnpm --filter @light/mobile exec tsc --noEmit` exits 0.
- `pnpm --filter @light/mobile run export` exits 0.
- `pnpm --filter @light/mobile test` passes (profileSchema).
- Grep gates in each task pass (self-scoped update, phone read-only, abha present).
</verification>

<success_criteria>
- Profile shows name/phone(read-only)/email/address/ABHA.
- Edits validate with zod and save via a self-scoped update, reflected in the session.
- Phone is never editable.
- MOB-05 satisfied in code; any patients_self_update backend gap documented.
</success_criteria>

<output>
After completion, create `.planning/phases/04-mobile-patient-app/04-04-SUMMARY.md`.
Document whether the patients_self_update policy was present (self-update succeeded) or needs deploying.
</output>
